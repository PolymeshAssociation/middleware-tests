#!/bin/bash

export CHAIN_VERSION="${CHAIN_VERSION:-5.1.0}"
export VAULT_CONTAINER_NAME="${VAULT_CONTAINER_NAME:-middleware-test-vault}"
export VAULT_ROOT_TOKEN="${VAULT_ROOT_TOKEN:-root}"
export VAULT_PORT="${VAULT_PORT:-8200}"